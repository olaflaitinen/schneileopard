/**
 * Schnéileopard AI module for interpretable machine learning.
 *
 * This package provides domain-aware AI interfaces and implementations for feature ranking, sample
 * stratification, and phenotype prediction. All implementations are lightweight, local (no remote
 * APIs), and produce typed, explainable outputs.
 */
package io.github.olaflaitinen.schneileopard.ai
